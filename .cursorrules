# FuickJS Framework 开发规范

你是一位正在维护 FuickJS 框架的高级开发人员。在执行每项任务时，请严格遵守以下规则。

## 1. 模块定义与开发模式

### 1.1 UI 组件 (Widgets)
- **JS 侧**: 在 `fuickjs/src/widgets/` 中添加组件。使用 TypeScript 和 React 开发模式。在 `fuickjs/src/widgets/index.ts` 中导出。
- **Flutter 侧**: 在 `fuickjs_flutter/lib/core/widgets/parsers/` 中实现对应的解析器 (Parser)。在 `FuickWidgetFactory` 中注册该解析器。
- **命名规范**: JS 组件和 Dart 解析器均使用 PascalCase (大驼峰) 命名法。

### 1.2 原生服务 (Native Services / Bridge)
- **JS 侧**: 在 `fuickjs/src/services/` 中定义类，使用 `dartCallNative` (同步) 或 `dartCallNativeAsync` (异步)。
- **Flutter 侧**: 在 `fuickjs_flutter/lib/core/service/` 中继承 `BaseFuickService` 实现。在 `NativeServiceManager` 中注册。
- **API 表面**: 确保 JS 和 Dart 之间的服务方法名称保持一致。

### 1.3 浏览器 API 补丁 (Browser API Polyfills)
- **位置**: 在 `fuickjs/src/ex/` 中实现。
- **注册**: 在 `fuickjs/src/runtime.ts` 的 `setupPolyfills` 函数中挂载到 `globalThis`。
- **兼容性**: 尽可能紧跟 W3C/WHATWG 标准。

### 1.4 渲染核心 (Rendering Core)
- 对 `renderer.ts`、`hostConfig.ts` 或 `PageContainer.ts` 的修改需极其谨慎，因为它们会影响所有 UI 组件和整体性能。

## 2. 文档编写要求 (强制)

**规则：每次修改或新增代码时，必须同步更新文档。**

- **新特性**: 在 `docs/` 目录下添加新的 `.md` 文件，解释特性功能、API 用法和架构设计。
- **修改**: 更新 `docs/` 中的现有文档，以反映行为或 API 的变更。
- **新增服务/组件**: 必须记录其属性 (Props)、方法 (Methods) 并提供使用示例。
- **校验**: 如果添加了新的文档文件，确保更新 `docs/README.md` 的索引。

## 3. 代码风格
- **TypeScript**: 使用严格类型检查。除非绝对必要，否则避免使用 `any`。
- **Dart**: 遵循 Flutter 代码检查 (Lint) 规则。使用 `logger` 进行调试。
- **Bridge**: 始终处理潜在的桥接错误，并向 JS 层提供有意义的错误信息。

## 4. 提交变更
- 在结束任务前，总结所做的更改，并验证 `docs/` 中的所有相关文档文件是否已更新或创建。
